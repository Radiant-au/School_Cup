# Port for the dev (npm run dev) and preview (npm run serve) servers.
# Defaults to 5173 when unset.
PORT=5173

# Base path the app is served from; also drives wouter's <Router base>.
# Defaults to / when unset.
BASE_PATH=/
